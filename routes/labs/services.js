const { User } = require('../../models/user');
const bcrypt = require('bcrypt')
const _ = require('lodash');
const crypto = require('crypto');

//post create cup for user(Admin)
const postCreateCupForUser = async (req, res) => {

}

module.exports = {
    postCreateCupForUser,
}